/**
 * Use for staging setup
 * @type {{apiHost: string, apiPort: number, couchDBHost: string, couchDBPort: number, couchDBName: string}}
 */
var env = {
    apiHost: 'https://app.shopeur.com.staging-2.oneba.se',
    apiPort: null,
    couchDBHost: 'https://couchdb.shopeur.com.staging-2.oneba.se',
    couchDBPort: null,
    couchDBName: 'shopeur'
};